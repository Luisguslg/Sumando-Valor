@page
@model ErrorModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Error</h1>
<h2 class="text-danger">Ha ocurrido un problema al procesar tu solicitud.</h2>
<p>Por favor, intente nuevamente más tarde. Si el problema persiste, contacte al administrador.</p>

@if (Model.ShowRequestId)
{
    <p class="text-muted small mt-3">
        <strong>Referencia:</strong> <code>@Model.RequestId</code>
    </p>
}
