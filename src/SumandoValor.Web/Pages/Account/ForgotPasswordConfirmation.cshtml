@page
@model ForgotPasswordConfirmationModel
@inject IWebHostEnvironment Environment
@{
    ViewData["Title"] = "Email Enviado";
}

<div class="text-center">
    <h2>Email de Recuperación Enviado</h2>
    <p class="text-muted mt-3">
        Si el email existe en nuestro sistema, recibirás un enlace para restablecer tu contraseña.
    </p>
    <p class="text-muted">
        Por favor revisa tu correo y sigue las instrucciones.
    </p>
    @if (Environment.IsDevelopment())
    {
        <p class="mt-3">
            <a asp-page="/Dev/Emails" class="btn btn-outline-primary">Ver Emails (Development)</a>
        </p>
    }
    <div class="mt-4">
        <a asp-page="./Login" class="btn btn-primary">Volver a Iniciar Sesión</a>
    </div>
</div>
